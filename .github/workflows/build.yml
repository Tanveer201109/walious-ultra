name: Build Flutter APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout কোড
        uses: actions/checkout@v4

      - name: Java 17 সেটআপ
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Flutter সেটআপ
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.x'
          channel: 'stable'

      - name: Dependencies ইনস্টল
        run: flutter pub get

      - name: সরাসরি Flutter দিয়ে APK বিল্ড (Victory Step)
        run: |
          echo "=== ব্যাকস্ল্যাশ পরিষ্কার করা ==="
          sed -i 's/https\\:\/\//https:\/\//g' android/gradle/wrapper/gradle-wrapper.properties
          
          echo "=== ক্লিনআপ ভেরিফাই করা ==="
          cat android/gradle/wrapper/gradle-wrapper.properties
          
          echo "=== ফ্লাটার বিল্ড শুরু (এটি ২-৫ মিনিট সময় নেবে) ==="
          flutter build apk --debug
          
      - name: APK আপলোড
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
          if-no-files-found: warn
